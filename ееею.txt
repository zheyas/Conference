{% extends 'base.html' %}

{% block title %}Документы конференции{% endblock %}

{% block content %}
<div class="container py-5">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="display-4 fw-bold text-primary mb-3">
                <i class="bi bi-file-earmark-text me-3"></i>
                Документы конференции
            </h1>
            <p class="lead text-muted">
                Здесь вы можете скачать все необходимые документы
            </p>
        </div>
    </div>

    <div class="row">
        {% for doc in documents %}
        <div class="col-md-6 col-lg-4 mb-4">
            <div class="card h-100 shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    <div class="d-flex align-items-center mb-3">
                        <div class="rounded-circle bg-primary bg-opacity-10 p-3 me-3">
                            <i class="bi bi-file-earmark-text text-primary fs-4"></i>
                        </div>
                        <h5 class="card-title fw-bold mb-0">{{ doc.title }}</h5>
                    </div>

                    <div class="mb-3">
                        <div class="d-flex justify-content-between text-muted small">
                            <span><i class="bi bi-calendar3 me-1"></i> {{ doc.uploaded_at|date:"d.m.Y" }}</span>
                            <span><i class="bi bi-hdd me-1"></i> {{ doc.get_file_size }}</span>
                        </div>
                    </div>

                    <a href="{% url 'docs:download_document' doc.id %}"
                       class="btn btn-primary w-100 py-2 rounded-3 d-flex align-items-center justify-content-center gap-2">
                        <i class="bi bi-download"></i>
                        Скачать документ
                    </a>
                </div>
            </div>
        </div>
        {% empty %}
        <div class="col-12">
            <div class="text-center py-5">
                <div class="display-1 text-muted mb-3">
                    <i class="bi bi-file-earmark-x"></i>
                </div>
                <h3 class="h5 text-muted">Нет доступных документов</h3>
                <p class="text-muted small">Документы появятся после добавления администратором</p>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
